module.exports = {
  detectPageLoadErrors(/* testHelpers, logErrors, isPerformanceTest */) {

    // add page error detection logic here and return an object: { error: string, pageLoaded: boolean }

    return {};

  },
  settled() {
    // add custom settled logic here
  },
};
